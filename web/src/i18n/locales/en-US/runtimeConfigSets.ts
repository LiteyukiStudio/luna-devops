const runtimeConfigSets = {
  tab: 'Config',
  title: 'Config',
  description: 'Extract runtime variables, config files, and secret files that multiple apps in this project space can reuse.',
  createTitle: 'Add config',
  editTitle: 'Edit config',
  dialogDescription: 'Config can be attached to deploy configs. It is injected into app containers on the next release or restart.',
  loadFailedTitle: 'Failed to load config',
  loadFailedDescription: 'Refresh the page or check project space permissions, then try again.',
  created: 'Config created',
  updated: 'Config updated',
  updatedNeedsRestart: 'Config updated. {{count}} deploy configs that live-reference it need a redeploy before it takes effect.',
  deleted: 'Config deleted',
  deleteTitle: 'Delete config',
  deleteDescription: 'This config will be deleted. Existing release history is not deleted automatically.',
  emptyTitle: 'No config',
  envVars: 'Plain config',
  envVarsHint: 'Public variables only. Supports a JSON object or one KEY=VALUE per line for reusable non-sensitive app config.',
  envVarsPlaceholder: 'LOG_LEVEL=info\nFEATURE_FLAG=true',
  configFiles: 'Config files',
  configFilesHint: 'Add one or more config files. Each file needs an absolute container path and file content.',
  configFilesPlaceholder: '/app/config.yaml',
  secretVariables: 'Secret variables',
  secretVariablesHint: 'Values are written only through the secure secret action and are never returned by the API. A secret takes precedence over a same-name public variable at runtime.',
  noSecrets: 'No secret variables are configured.',
  saveBeforeSecrets: 'Save this config first, then add or manage secret variables here.',
  secretsUpdated: 'Secret variables updated.',
  secretsUpdateFailed: 'Secret variables could not be updated.',
  secretFiles: 'Secret files',
  secretFilesHint: 'Add one or more secret files. Values are not shown after saving; useful for certificates and private keys.',
  secretFilesConfiguredHint: 'Secret files are configured. Saving without adding files keeps them; adding files replaces the current secret file set.',
  secretFilesPlaceholder: '/app/certs/client.key',
  configFileState: '{{count}} config lines',
  configured: 'Configured',
  notConfigured: 'Not configured',
}

export default runtimeConfigSets
