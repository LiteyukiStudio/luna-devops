# Configuration

Each installation method supplies most defaults. Review configuration by component:

- [API](./configuration/api)
- [Worker](./configuration/worker)
- [Agent](./configuration/agent)

Each page starts with basic settings, followed by every remaining advanced setting. Inject keys, passwords, and authentication headers through Secrets; never commit them to a repository or plaintext values file.
