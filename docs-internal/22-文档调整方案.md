# 全局优化项

文档尽量和项目功能对齐，请检查一遍

# 顶部主菜单调整

{开始 使用 参考} 改为 {安装 使用 参考}

## 安装nav（默认阅读该nav下页面的人有一定运维和开发能力）

### 安装平台

- Kubernetes / K3s 改为 Kubernetes (Helm)。然后仅在页面内说明兼容其他k8s实现/发行版

- 直接运行二进制 改为 二进制

### 第一次登录

第一次登录菜单 改为 初始化

### 连接集群和镜像站

连接集群和镜像站菜单 改为 配置基础资源

里面的内容为添加集群，镜像站，配置GitProvider的OAuth，方便其他用户能开箱就用


### 移除剩下的菜单

- 上线第一个项目
- 启动排障
- 常见问题

## 使用Nav（面向用户而不是开发者，默认阅读该nav下页面的人有基本开发能力，知道自己项目应该怎么跑起来，但是对运维和部署了解的不多）

目前问题：该nav下菜单过多，需要适当删减和合并
某些大段文本过于长篇大论，看起来没有重点，可以适当用粗体斜体，引用，代码块等markdown组件来改善这种情况
有些文档过于冗余讲述代码设计细节，实际上用户根本不关心，这类内容需要删除

### 日常交付

> 这个文档目前还不错，但是内容过于冗余，可以大幅度瘦身

先让用户了解项目空间是类似命名空间起一个逻辑上的隔离作用

再引导用户知道需要创建应用

创建应用后需要创建部署

创建完部署后根据情况来进行构建或者直接拉取镜像部署

最后根据需求进行路由创建什么的

推荐可以再拆一层子页面来实现每个步骤

### AI助手

> 这个文档严重冗余，文档是面向使用者而不是开发者的，讲了太多我们这个AI助手的机制

我们只需要告诉用户有AI助手可以使用即可，以及AI助手的能力边界，还有一些信息安全相关的注意事项

### 应用市场

> 这个再优化一下

### 配置与密钥 & 部署配置导入与导出

这两个放到日常交付的子页面下面

### 项目数据卷 & 数据卷导入与导出 & 数据保留与清理

这几个合并为 运行数据持久化，并且检查内容和修改，放到日常交付的子页面下面

### 移除菜单

- 消息盒子：我想是个人都知道怎么用
- 默认定价与建议价格：这个放到安装里面，是给平台管理员看的，然后改为价格配置

### 安装 & 使用Luna CLi

这俩菜单有点问题，我不知道是不是有意为之，貌似点击后就只剩这两个了，不要这种效果，不好

## 参考

### 环境变量

这个页面放到安装里面，改为配置项

并且环境变量表格的类型移除，这个没啥太大用

### 可观测平台

改为 可观测

并且该文档严重冗余，文档是面向使用者而不是开发者的，讲了太多我们这个可观测平台的机制，只需要说明怎么配置即可

### 数据卷传输

这个功能我都不知道，文档删了，除非你给我一个不能删的理由

### 网关流量探针

这个文档也有点冗余，文档是面向使用者而不是开发者的，讲了太多我们这个网关流量探针的机制，只需要说明怎么配置即可，机制几句话就行，放到最后