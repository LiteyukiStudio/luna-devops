# 用户场景测试

本文合并用户场景模拟路径与场景测试问题记录。

---

# 场景模拟
## 验证原则

- 以真实用户路径为准，不只验证单个 API 或单个页面。
- 优先覆盖 MVP 基本功能：认证、项目空间、应用、部署配置、代码仓库、镜像站、构建、部署、访问、运行配置、权限和审计。
- 平台尚未上线，本地数据可清空重建；不为旧数据或旧路由保留兼容路径。
- 所有敏感值只验证“可写入、不可回显、可替换”，不在文档中记录真实 Secret。
- 场景验证时先跑主路径，再补失败路径和权限边界。
- 每次场景验证都必须同步维护本文「场景测试问题记录」一节，按批次记录问题、修复动作和复测结论。

## 通用前提

- 平台管理员已经完成首次初始化，并能登录后台。
- 至少存在一个可用 Kubernetes / K3s 集群，状态为 healthy。
- 至少存在一个可用镜像站，例如 Harbor、Gitea Registry 或 DockerHub。
- 至少存在一个 Git Provider，例如 GitHub 或 Gitea，并已配置 OAuth App 或允许 PAT 授权。
- Builder Agent 已注册，scope 能覆盖测试项目空间，标签能匹配默认构建配置。
- Worker 正常运行，Redis / PostgreSQL 可用。
- 测试用户包括：
  - `owner-user`：项目空间 Owner。
  - `dev-user`：项目空间 Developer。
  - `viewer-user`：项目空间 Viewer。

## 场景一：管理员完成平台基础配置

### 目标

验证平台管理员能完成全局基础资源配置，使普通用户后续可以创建应用并交付。

### 覆盖功能

登录、站点设置、OIDC Provider、Git Provider、镜像站、集群、Builder、系统状态展示。

### 操作路径

1. 管理员登录平台。
2. 设置站点标题、Logo、登录页副标题和默认语言。
3. 创建或确认 OIDC Provider，并保存准入策略。
4. 创建 Git Provider，填写实例地址、Client ID 和 Client Secret。
5. 创建全局镜像站，配置 endpoint、类型和默认凭据。
6. 接入 Kubernetes / K3s 集群，测试 kubeconfig。
7. 确认 Builder Agent 在线，scope 和标签可被项目空间使用。
8. 返回看板，查看集群、镜像站、构建器和近期状态。

### 期望结果

- 管理员能看到所有全局资源的健康状态。
- Client Secret、Git Token、Registry Credential 不回显明文。
- 看板可以展示集群、镜像站、Builder 的状态摘要。
- 普通用户不应在业务入口看到管理员维护的敏感连接配置。

## 场景二：个人开发者交付 monorepo 博客

### 目标

验证一个用户从代码仓库创建两个应用，并分别用 dev / prod 部署配置完成构建和部署。

### 用户故事

用户自己开发了一个博客系统https://github.com/snowykami/neo-blog，仓库是 monorepo，包含前端和后端。后端在仓库根目录，前端在 `web/` 目录下，两个应用都已经写好 Dockerfile。用户希望 dev 分支提交自动部署到开发环境，main 分支合并后自动部署到生产环境。

### 覆盖功能

项目空间、应用、仓库绑定、部署配置、Dockerfile 探测、构建上下文、分支触发、自动部署、发布记录、回滚、访问入口。

### 操作路径

1. `owner-user` 创建项目空间：`轻雪的博客`。
2. 在项目空间内创建两个环境：
   - `开发环境`，标识 `dev`。
   - `生产环境`，标识 `prod`。
3. 绑定 Git 凭据并选择博客仓库。
4. 创建应用 `blog-api`，图标选择 server。
5. 在 `blog-api` 下创建部署配置 `开发`：
   - 来源类型：代码仓库。
   - 仓库：博客仓库。
   - 分支规则：`dev`。
   - Dockerfile：`Dockerfile`。
   - 构建上下文：`.`。
   - 环境：`开发环境`。
   - 自动部署：开启。
6. 在 `blog-api` 下创建部署配置 `生产`：
   - 分支规则：`main`。
   - 环境：`生产环境`。
   - 自动部署：开启或需要人工确认，按产品当前能力选择。
7. 创建应用 `blog-web`。
8. 在 `blog-web` 下创建 dev / prod 两个部署配置：
   - Dockerfile：`web/Dockerfile`。
   - 构建上下文：`web`。
   - dev 匹配 `dev`，prod 匹配 `main`。
9. 手动触发一次 `blog-api` dev 构建，查看 BuildRun、BuildJob、日志和最终镜像。
10. 手动触发一次 `blog-web` dev 构建，确认目标镜像模板和镜像站凭据生效。
11. 模拟 dev 分支 webhook，确认平台只触发 dev 部署配置。
12. 模拟 main 分支 webhook，确认平台只触发 prod 部署配置。
13. 在部署页查看 Release 状态、部署日志和当前镜像。
14. 为前端 dev 部署配置创建默认域名访问入口。
15. 对 prod 发布执行一次回滚。

### 期望结果

- 一个 monorepo 通过多个应用表达多个可部署服务，而不是引入额外模块层级。
- BuildRun 直接绑定 `applicationId` 和 `deploymentTargetId`。
- dev / prod 构建和部署不会互相串线。
- 构建成功后能看到镜像引用、digest、触发人、Git 提交者和日志。
- 自动部署会生成 Release，发布失败时能看到业务化错误和日志。
- 访问入口直接选择部署配置，不要求用户单独选择环境和阶段。
- 回滚只允许回滚到同一应用、同一环境、同一部署配置的上一成功 Release。

## 场景三：只有镜像的第三方服务直接部署

### 目标

验证用户不绑定代码仓库，也能通过已有镜像创建应用并发布。

### 用户故事

团队需要在项目空间里运行一个第三方管理后台，例如 `nginx`、`adminer` 或内部已构建好的镜像。这个服务没有源码仓库，也不需要平台 Builder 构建。

### 覆盖功能

镜像站、镜像记录、应用、镜像来源部署配置、手动发布、运行配置、访问入口。

### 操作路径

1. `owner-user` 在已有项目空间中创建应用 `adminer`。
2. 创建部署配置 `开发`：
   - 来源类型：已有镜像。
   - 镜像引用：选择镜像站中的 `adminer:latest`，或手动填写完整镜像引用。
   - 环境：`开发环境`。
   - 自动部署：关闭。
3. 在部署页创建 Release，使用该镜像引用部署。
4. 查看 Release 运行日志和 Kubernetes 资源。
5. 创建访问入口，使用默认域名访问服务。
6. 修改镜像 tag 为新版本，再创建一次 Release。
7. 回滚到上一成功版本。

### 期望结果

- `sourceType=image` 的部署配置不进入 Builder 队列。
- 没有仓库绑定时，应用仍然可以完成发布、访问和回滚。
- Release 记录包含镜像引用、环境、部署配置和操作人。
- 集群资源归属展示为“项目空间 / 应用 / 部署配置”，不展示原始 ID 作为主要信息。

## 场景四：运行配置和密钥覆盖

### 目标

验证 Environment 默认配置与 DeploymentTarget 覆盖项能合并生成运行态 ConfigMap / Secret。

### 覆盖功能

环境、部署配置、ConfigMap、Secret、Secret 不回显、重新发布、集群资源查看。

### 操作路径

1. `owner-user` 在项目空间环境 `开发环境` 中配置默认运行参数：
   - `LOG_LEVEL=info`。
   - `FEATURE_FLAG=false`。
2. 在应用 `blog-api` 的 dev 部署配置中添加覆盖项：
   - ConfigMap：`FEATURE_FLAG=true`。
   - Secret：`DATABASE_URL=<测试数据库连接串>`。
3. 保存后重新打开编辑页，确认 Secret 明文不回显。
4. 创建或重新发布 dev Release。
5. 在集群资源页面查看该部署配置生成的 ConfigMap、Secret、Deployment 和 Service。
6. 修改 Secret 值为空保存，确认含义是“不修改已有 Secret”。
7. 输入新的 Secret 值保存，再发布一次。

### 期望结果

- 同名普通配置以部署配置覆盖项为准。
- Secret 只允许写入，不在 API 和前端回显明文。
- 发布时 Worker 使用“环境默认 + 部署配置覆盖”生成运行态资源。
- 集群资源能按项目空间、应用和部署配置筛选或识别。

## 场景五：项目协作与权限边界

### 目标

验证 Owner、Developer、Viewer 的权限边界，以及危险操作审计。

### 覆盖功能

成员管理、角色权限、按钮显隐、后端权限校验、AuditLog、危险操作确认。

### 操作路径

1. `owner-user` 邀请 `dev-user` 加入项目空间，角色为 Developer。
2. `owner-user` 邀请 `viewer-user` 加入项目空间，角色为 Viewer。
3. `dev-user` 登录后尝试：
   - 创建应用。
   - 创建部署配置。
   - 手动触发 dev 构建。
   - 部署 dev Release。
   - 删除应用。
   - 管理项目级镜像站。
4. `viewer-user` 登录后尝试：
   - 查看项目空间、应用、构建和发布。
   - 触发构建。
   - 修改部署配置。
   - 查看 Secret。
5. `owner-user` 尝试删除应用，确认需要二次确认。
6. 管理员查看 AuditLog。

### 期望结果

- Developer 可以创建应用、维护交付配置、构建和部署非生产环境。
- Developer 不可以删除应用或管理项目级镜像站。
- Viewer 只能查看，不可以触发构建、部署或写入配置。
- 前端隐藏无权限操作，但后端仍必须最终拒绝越权请求。
- 删除、成员变更、Secret 写入、Token 操作等危险操作必须产生审计记录。

## 场景六：Access Token 触发自动化构建

### 目标

验证外部系统可以使用平台 Access Token 触发构建，同时受 scope 和项目权限限制。

### 覆盖功能

Access Token、scope、API 触发构建、BuildRun 记录、Token 撤销。

### 操作路径

1. `owner-user` 在账号页创建 Access Token，只选择构建触发所需 scope。
2. 保存 Token，确认 Token 只展示一次。
3. 使用该 Token 调用构建触发 API，指定项目空间、应用和部署配置。
4. 查看应用构建页，确认出现 triggerType=`api` 的 BuildRun。
5. 尝试用同一个 Token 调用无 scope 的 API，例如删除应用或管理成员。
6. 撤销 Token。
7. 再次调用构建触发 API。

### 期望结果

- Token 只保存 hash，不再回显原文。
- 有 scope 的构建触发可以成功进入队列。
- 无 scope 的 API 默认拒绝。
- 撤销后的 Token 不能继续使用。
- BuildRun 能展示 API 触发来源，不冒充 Git 提交者。

## 场景七：失败诊断和恢复

### 目标

验证平台在常见失败情况下能给出可理解的状态、日志和恢复入口。

### 覆盖功能

构建失败、镜像推送失败、部署失败、网关失败、日志、重试、回滚。

### 操作路径

1. 创建一个 Dockerfile 路径错误的部署配置，触发构建。
2. 查看 BuildRun 状态、BuildJob 日志和错误信息。
3. 修正 Dockerfile 路径，重试构建。
4. 临时配置错误镜像站凭据，触发构建推送失败。
5. 恢复镜像站凭据，重试构建。
6. 创建一个服务端口错误的部署配置并发布，观察访问或 rollout 失败。
7. 修正部署配置服务端口或镜像配置后重新发布。
8. 创建一个 DNS/CNAME 未验证的自定义域名，查看访问入口状态。

### 期望结果

- 构建失败、部署失败、网关失败都能在对应页面看到状态 Badge 和日志入口。
- 失败信息不直接暴露底层堆栈或 Secret。
- 终态构建可以重试；成功历史发布可以用于回滚。
- 网关和证书状态能独立展示，避免用户误以为应用部署失败。

## 场景确认后建议的模拟顺序

1. 先跑场景一，确认平台基础资源可用。
2. 再跑场景二，验证源码构建到部署的主闭环。
3. 再跑场景三，验证无源码镜像直部署。
4. 再跑场景四，验证运行配置和 Secret。
5. 再跑场景五和场景六，验证权限和外部 API。
6. 最后跑场景七，验证失败路径和恢复体验。

## 待确认问题

- prod 环境是否默认要求人工确认，还是 MVP 中允许自动部署。
- PR Merge 事件是否在当前阶段直接等价为 main 分支 push webhook。
- 自定义域名验证是否要求真实 DNS，还是本地模拟只验证 CNAME 提示和状态流转。
- 本地场景验证是否允许清空数据库和重新部署 dev compose。

## 模拟记录：2026-06-11 Chrome 本地验证

### 本轮范围

- 使用 Chrome 访问本地前端 `http://localhost:5173`。
- 使用最近登录账号 `Platform Admin <admin@luna.dev>` 恢复登录。
- 不测试新增集群、代码库、镜像站配置；这些资源按已有本地配置处理。
- 重点验证项目空间、应用详情、构建、部署、访问、集群资源归属等核心 DevOps 路径。

### 已跑通部分

1. 登录和看板
   - 最近登录账号可以直接恢复到 `/dashboard`。
   - 看板能展示项目空间、应用、近期构建、健康集群、镜像站和 Builder 摘要。
   - Chrome 控制台未出现前端 error。

2. 应用详情基础浏览
   - 可进入 `Snowykami的项目 / 博客` 应用详情页。
   - Topbar 的项目空间名和应用名可点击，并保持“项目空间 / 应用”的层级。
   - 概览、构建、部署、访问四个 tab 可切换。

3. 构建列表浏览
   - 构建 tab 能展示历史 BuildRun、状态 Badge、分支、镜像、触发人、Git 提交者、耗时、分页和筛选器。
   - 构建记录行的仓库、提交和镜像引用展示正常。

4. 访问入口创建
   - 访问 tab 初始为空时可以点击“创建域名”。
   - 域名表单只选择部署配置，环境由部署配置推导，不再单独选择环境或阶段。
   - 留空域名后保存成功，生成默认域名 `neo-blog-blog-dev.apps.local`。
   - 路由创建后列表展示域名、路径、TLS 模式、状态和检查 / 编辑 / 删除操作。

5. 集群资源归属展示
   - 工作负载、服务与入口、配置与密钥 tab 能读取 `Local Minikube` 资源。
   - 新模型资源能展示可读归属，例如 `Snowykami的项目 / 博客 / 后端部署`。
   - Secret 列表只展示 `data hidden`，没有回显密钥内容。

### 阻塞和体验卡点

1. 新应用创建失败
   - 操作：在项目空间应用 tab 中创建 `场景测试应用 / scenario-app`。
   - 结果：提交后 toast 报错：
     `ERROR: null value in column "source_type" of relation "applications" violates not-null constraint (SQLSTATE 23502)`。
   - 判断：应用最小单元重构后，前端创建应用只提交基础信息，但数据库或模型里仍残留旧 `applications.source_type` 非空约束。
   - 影响：场景二、场景三无法从 UI 新建应用，主路径被阻断。

2. 应用详情缺少创建部署配置入口
   - 操作：进入已有应用详情页，检查概览、构建、部署、访问 tab 的可用动作。
   - 结果：页面只出现“保存配置”“触发构建”“创建发布”“创建域名”，没有“创建部署配置 / 新增部署配置”入口。
   - 判断：旧模块页签移除后，新的 DeploymentTarget 创建和编辑入口没有补齐到应用详情工作流。
   - 影响：即使应用创建成功，用户也无法通过 UI 完成“应用 -> 部署配置 -> 构建/部署”的核心配置。

3. 现有部署配置数据不完整，导致触发构建禁用
   - 现象：已有应用的构建 tab 中“触发构建”按钮为 disabled。
   - 数据盘点：当前 `deployment_targets` 存在记录，但 `repository_binding_id`、`build_provider_id`、`target_registry_id`、`target_repository` 等关键字段为空。
   - 判断：本地历史数据来自旧模型，无法满足新模型触发构建所需配置。
   - 影响：无法用现有应用跑新 BuildRun，也无法验证自动部署链路。

4. 历史 BuildRun / Release 无部署配置归属
   - 现象：应用概览显示存在成功发布记录，但部署健康显示 `0 / 1 个部署成功`；部署 tab 中部署配置显示“未部署”。
   - 数据盘点：历史 `build_runs.deployment_target_id` 和 `releases.deployment_target_id` 为空。
   - 影响：新部署页无法把历史构建和发布归属到部署配置，行内“部署”按钮禁用。
   - 说明：这符合“不兼容旧数据”的原则，但本地若要继续完整验收，需要清库重建或补一组新模型数据。

5. 顶部“创建发布”和行内“部署”的状态不一致
   - 部署列表行内“部署”按钮禁用。
   - 顶部“创建发布”按钮仍可打开弹窗，并列出旧构建记录。
   - 选择旧构建后能带出镜像，但部署配置字段仍为 `-`，保存保持 disabled。
   - 体验问题：用户容易误以为可以从顶部创建发布，但最终不知道为什么不能保存。

6. 创建应用弹窗描述仍是旧口径
   - 弹窗描述为“先支持 repository 和 image 两类来源，部署和构建后续联调。”
   - 当前产品口径是“应用只维护基础信息；repository / image 来源归属部署配置”。
   - 影响：新用户会误解应用创建阶段需要选择来源类型。

7. 旧 Kubernetes 资源仍显示部署配置为 `-`
   - 工作负载、服务、ConfigMap、Secret 中，新资源归属能显示部署配置名。
   - 旧资源仍显示 `Snowykami的项目 / 博客 / -`。
   - 说明：由于本轮已移除 legacy label fallback，这是预期结果；完整验收前建议清理旧资源或重建本地集群资源。

8. 看板项目空间图钉按钮泄漏 i18n key
   - 现象：看板项目空间卡片中出现 `common.pinProject` / `common.unpinProject` 文案。
   - 判断：对应按钮的 tooltip 或 aria-label 缺少翻译资源。
   - 影响：不阻断核心链路，但会降低本地试用时的完成度和可信度。

9. 应用概览部署配置指标重复
   - 现象：应用概览运行态摘要中“部署配置”指标重复出现，且含义相近。
   - 判断：应用概览在移除模块概念后，部分指标映射没有重新收敛。
   - 影响：用户会误以为有两类不同部署配置，需要在下一轮 UI polish 中合并或改名。

### 本轮结论

核心 DevOps 主路径尚未跑通。当前最大的阻塞不是集群、代码库或镜像站配置，而是应用最小单元重构后的新模型入口不完整：

- 后端 / 数据库仍阻止基础应用创建。
- 前端缺少部署配置创建和编辑入口。
- 现有本地历史构建和发布无法映射到新部署配置。

### 修复记录：2026-06-11

- 已清理应用表旧来源字段残留，当前应用创建不再依赖 `source_type`。
- 应用部署页已补齐部署配置新增、编辑、删除入口，表单支持代码仓库来源和已有镜像来源。
- 创建发布入口已按新模型收紧：只有存在同部署配置成功构建，或已有镜像部署配置带有镜像引用时才可发布；无 `deploymentTargetId` 的历史 BuildRun 不再进入候选。
- 已修复创建应用弹窗旧口径、看板图钉 i18n key 泄漏、应用概览部署配置指标重复问题。
- 仍需在本地验收数据重建后继续跑完整主路径，覆盖 repository 构建、image 直部署、自动部署、访问入口、运行配置和回滚。

建议先修复应用创建和部署配置维护入口，再清空本地旧库或构造一组纯新模型数据，重新执行场景二、三、四。

## 模拟记录：2026-06-11 Neo Blog 浏览器部署试跑

### 本轮范围

- 仅通过浏览器模拟用户操作，不直接操作数据库。
- 先在项目空间列表通过 UI 删除旧项目空间，再检查集群资源页确认平台可见资源状态。
- 使用仓库 `snowykami/neo-blog` 的真实部署诉求建模：前端、后端、数据库放在同一个项目空间下。
- 代码库和镜像站新增配置暂不测试；由于清理后 UI 没有仓库绑定创建入口，本轮改用已有镜像直部署路径继续验证部署链路。

### 已完成操作

1. 通过 UI 删除旧项目空间数据，项目空间列表显示空状态。
2. 通过 UI 检查集群资源：
   - 工作负载、服务与入口、配置与密钥、命名空间、存储 tab 均可打开。
   - 初始状态未看到平台管理的旧工作负载、服务、入口、配置、密钥或命名空间。
3. 通过 UI 创建项目空间 `Neo Blog 场景测试`，标识 `neo-blog`。
4. 通过 UI 创建环境 `开发环境`，标识 `dev`，阶段 `dev`，副本 1，资源规格 `100m / 256Mi`。
5. 通过 UI 创建应用：
   - `Neo Blog 前端`，标识 `neo-blog-frontend`。
   - `Neo Blog 后端`，标识 `neo-blog-backend`。
   - `Neo Blog 数据库`，标识 `neo-blog-db`。
6. 通过 UI 创建镜像来源部署配置：
   - 数据库：`bitnami/postgresql:16`，配置 `POSTGRESQL_DATABASE/USERNAME/PASSWORD` 和 `POSTGRESQL_PORT_NUMBER=8080`。
   - 后端：`snowykami/neo-blog-backend:latest`，预置数据库连接环境变量，待数据库 Service 名称确认后替换 `DB_HOST`。
   - 前端：`snowykami/neo-blog-frontend:latest`，预置 `PORT=8080` 和 `BACKEND_URL=http://neo-blog-api.localhost`。

### Neo Blog 配置确认

从 `snowykami/neo-blog` 仓库 README、`.env.example`、Dockerfile 和 Helm values 确认：

- 后端镜像：`snowykami/neo-blog-backend:latest`。
- 前端镜像：`snowykami/neo-blog-frontend:latest`。
- 后端默认监听端口：`8888`；Helm 示例中可通过 `PORT=80` 改为 80。
- 前端默认监听端口：`3000`；Next standalone 镜像支持 `PORT` 和 `HOSTNAME`。
- 前端服务端访问后端使用 `BACKEND_URL`，生产容器示例为 `http://neo-blog-backend:8888`，Helm 示例为内网后端 Service。
- 后端外部访问基准地址使用 `BASE_URL`，用于生成外部 URL。
- 后端数据库支持 `sqlite` 和 `postgres`；本轮按主人要求同时部署 Postgres，并把前端、后端、数据库放在同一个项目空间下。

本轮重跑建议配置。服务端口填写在各应用的部署配置中，应用基础信息只维护名称、标识和图标：

| 应用 | 部署配置服务端口 | 来源 | 环境变量 |
| --- | --- | --- | --- |
| `neo-blog-db` | `5432` | `postgres:16-alpine` | `POSTGRES_DB=blog`、`POSTGRES_USER=blog`、`POSTGRES_PASSWORD=blog` |
| `neo-blog-backend` | `80` | `neo-blog-backend:devtest` | `PORT=80`、`MODE=prod`、`BASE_URL=http://blogtest.local`、`DB_DRIVER=postgres`、`DB_HOST=<db-service-name>`、`DB_PORT=5432`、`DB_USER=blog`、`DB_PASSWORD=blog`、`DB_NAME=blog`、`DB_SSLMODE=disable`、`CAPTCHA_TYPE=disable`、`EMAIL_ENABLED=false`、`PASSWORD_SALT=dev-salt`、`JWT_SECRET=dev-jwt-secret` |
| `neo-blog-frontend` | `3000` | `neo-blog-frontend:devtest` | `PORT=3000`、`HOSTNAME=0.0.0.0`、`BACKEND_URL=http://<backend-service-name>:80` |

访问入口只暴露前端部署配置，域名使用 `blogtest.local`，TLS 暂不配置。

### 阻塞和体验卡点

1. 清理后仓库绑定无法通过 UI 重建
   - 现象：`/code-repositories` 只有 Git Provider 和 Git 凭据 tab；项目空间内仓库绑定路由不可用或返回空壳。
   - 影响：无法从浏览器完整走 `snowykami/neo-blog` 的 repository 构建路径，只能临时改走镜像直部署验证。

2. 项目空间删除后分页总数未刷新
   - 现象：项目空间列表已经显示“还没有项目空间”，底部分页仍显示旧的总数。
   - 影响：用户会误以为仍有隐藏数据残留。

3. 顶部“创建发布”容易创建无部署配置归属的发布
   - 操作：在数据库应用部署页点击顶部“创建发布”。
   - 结果：发布进入 Worker 后生成兜底资源名 `dplt`，集群资源归属显示为 `Neo Blog 场景测试 / Neo Blog 数据库 / -`。
   - 影响：该错误资源没有可见 UI 删除入口，后续多个部署配置可能冲突；已将顶部按钮改为显式使用第一个可发布部署配置打开发布弹窗。

4. 集群资源页缺少孤儿资源清理入口
   - 现象：工作负载和服务列表只能查看和刷新，不能对平台管理资源执行删除或重新同步。
   - 影响：一旦错误发布创建了 `dplt` 这类无部署配置归属资源，用户无法只通过 UI 清理。

5. 数据库镜像部署未 ready，页面诊断信息不足
   - 现象：发布日志持续显示 `rollout 进行中：updated=1 ready=0 available=0 desired=1`，工作负载页显示 Pod 为“待处理”，但没有 Pod events、容器日志、镜像拉取状态或调度失败原因。
   - 影响：用户无法仅靠平台 UI 判断是镜像拉取、端口、资源、启动参数还是调度问题。

### 本轮结论

本轮首次试跑完成清库后的纯 UI 重建，并验证应用、环境、镜像来源部署配置可以创建；首次未跑通，主要阻塞是仓库绑定入口缺失、错误发布资源无法通过 UI 清理、部署失败诊断信息不足。后续修复后已在“Neo Blog 清库重跑验收”完成数据库、后端、前端和访问入口闭环。

## 模拟记录：2026-06-11 Neo Blog 清库重跑验收

### 本轮范围

- 主人确认允许清库后，清理本地业务数据、Redis 队列和旧 Kubernetes 残留资源。
- 使用浏览器创建 `Neo Blog 验收` 项目空间、`dev` 环境、数据库 / 后端 / 前端应用。
- 通过浏览器创建镜像来源部署配置和发布。
- 通过浏览器创建 `blogtest.local` 访问入口，并使用 Chrome 验证页面。

### 已完成操作

1. 清理本地业务数据和旧 Kubernetes 残留资源，保留已有认证、Git、镜像站、集群和 Builder 配置。
2. 通过 UI 创建项目空间 `Neo Blog 验收`，标识 `neoblogtest`。
3. 通过 UI 创建环境 `开发环境`，标识 `dev`，阶段 `dev`，副本 1，资源规格 `100m / 256Mi`。
4. 通过 UI 创建应用：
   - `Neo Blog 数据库`，标识 `neo-blog-db`。
   - `Neo Blog 后端`，标识 `neo-blog-backend`。
   - `Neo Blog 前端`，标识 `neo-blog-frontend`。
5. 通过 UI 创建并发布数据库部署配置：
   - 镜像 `postgres:16-alpine`。
   - 服务端口 `5432`。
   - 环境变量 `POSTGRES_DB=blog`、`POSTGRES_USER=blog`、`POSTGRES_PASSWORD=blog`。
6. 通过 UI 创建并发布后端部署配置：
   - 镜像 `neo-blog-backend:devtest`。
   - 服务端口 `80`。
   - 关键环境变量：`PORT=80`、`BASE_URL=http://blogtest.local`、`DB_DRIVER=postgres`、`DB_HOST=dplt-f5a2cceb13`、`DB_PORT=5432`、`DB_USER=blog`、`DB_PASSWORD=blog`、`DB_NAME=blog`。
7. 通过 UI 创建并发布前端部署配置：
   - 镜像 `neo-blog-frontend:devtest`。
   - 服务端口 `3000`。
   - 环境变量 `PORT=3000`、`HOSTNAME=0.0.0.0`、`BACKEND_URL=http://dplt-5540420c06:80`。
8. 通过 UI 创建访问入口：
   - 部署配置 `前端开发`。
   - 域名 `blogtest.local`。
   - 路径 `/`。
   - 服务端口 `3000`。
   - TLS 模式 `http-only`。

### 验收结果

- Postgres、后端、前端 Deployment 均 `1/1 Running`。
- Ingress `luna-gateway-gwr-3d76bbf25dc5d2828373b40a` 已绑定 `blogtest.local`，后端服务为 `dplt-6cdf13c21b:3000`。
- 本地访问使用 minikube 已有映射：`http://blogtest.local:64111/`。
- `curl` 返回 `200 OK`。
- Chrome 验证页面标题为 `首页 - Snowykami's Blog`，首页出现“最新文章”。

### 体验卡点

1. 公共后端镜像 `snowykami/neo-blog-backend:latest` 缺少 `linux/arm64` manifest，本地 ARM minikube 无法直接拉取；本轮改为源码构建 `neo-blog-backend:devtest` 并加载到 minikube。
2. 原 ingress-nginx addon 镜像源中的 controller digest 不存在，导致控制器长期 `ImagePullBackOff`；本轮切换到 `registry.k8s.io/ingress-nginx/controller:v1.14.3`。
3. 部署配置编辑保存后，部署列表目标镜像未立即刷新，但创建发布弹窗可以带出更新后的镜像；需要后续补查询失效或乐观更新。
4. 平台 UI 已能展示 Pod `ImagePullBackOff` 摘要，并已补充部署页运行日志入口；完整 Pod events、重新同步入口和交互式 TTY Web Console 仍需后续增强。

### 本轮结论

Neo Blog 镜像直部署链路已跑通，可以交给主人通过 `http://blogtest.local:64111/` 验收。后续场景应继续覆盖 repository 构建、自动部署、回滚、ConfigMap/Secret 编辑、运行日志 / Web Console 复测和更完整的部署诊断。

## 模拟记录：2026-06-14 Browser 镜像直部署运行配置闭环

### 本轮范围

- 使用 Codex in-app Browser 访问本地前端 `http://localhost:5173`。
- 不清库，从 UI 新建 `浏览器闭环测试` 项目空间。
- 验证场景三“已有镜像直接部署”和场景四“运行配置和密钥覆盖”的主要路径。
- 复测部署页运行日志和 Web Console。
- 验证集群资源页和 ingress 访问。

### 已完成操作

1. 登录态恢复正常，当前账号为 `Platform Admin <admin@luna.dev>`。
2. 看板显示 1 个健康集群、2 个镜像站、1 个在线构建器。
3. 创建项目空间 `浏览器闭环测试`，标识 `browser-loop-test`。
4. 创建环境 `开发环境`，标识 `dev`，集群选择 `Local Minikube`。
5. 创建应用 `Nginx 演示`，标识 `nginx-demo`。
6. 创建部署配置 `开发部署`：
   - 来源类型：已有镜像。
   - 镜像：`nginx:1.27-alpine`。
   - 服务端口：`80`。
   - 普通配置：`APP_ENV=dev`、`FEATURE_FLAG=true`。
   - 附加 ConfigMap：`NGINX_ENTRYPOINT_QUIET_LOGS=1`。
   - 配置文件：`/etc/nginx/conf.d/default.conf`。
7. 创建 Release `#1` 并等待发布成功。
8. 在部署页查看部署日志和运行日志。
9. 在 Web Console 中执行命令，确认环境变量和配置文件已经进入容器。
10. 创建访问入口 `nginx-loop.local`，路径 `/`，TLS 模式 `http-only`。
11. 在集群资源页查看 Deployment、Pod、Service、Ingress、ConfigMap、Secret。

### 验收结果

- Release 运行状态为“就绪”，发布状态为“成功”。
- Deployment `dplt-8d88907683` 为 `1/1 Running`。
- Service `dplt-8d88907683` 暴露 80 端口。
- ConfigMap `dplt-8d88907683-config` 生成 3 个 key。
- ConfigMap 文件 `dplt-8d88907683-config-files` 生成 1 个 key。
- Secret `dplt-8d88907683-secret` 在 UI 中只显示 `data hidden`。
- Web Console 可进入容器 shell，并读到 `APP_ENV=dev FEATURE_FLAG=true NGINX_ENTRYPOINT_QUIET_LOGS=1`。
- Ingress `nginx-loop.local` 已下发，`curl --resolve nginx-loop.local:64111:127.0.0.1 http://nginx-loop.local:64111/` 返回 `luna browser loop ok`。

### 体验卡点

1. 项目空间列表仍有分页 total 异常：空状态显示“还没有项目空间”，但分页显示有记录；新建后只展示 1 行却显示“共 2 条”。
2. 集群资源页归属只展示“项目空间 / 应用”，未展示部署配置名 `开发部署`。
3. 已创建的域名点击“检查域名”后提示“域名已被占用”，容易误导用户。
4. in-app Browser 直接打开 `nginx-loop.local:64111` 被浏览器侧拦截，本轮用 `curl --resolve` 完成最终 HTTP 内容验证。

### 本轮结论

镜像直部署和运行配置主链路已闭环，运行日志与 Web Console 已通过真实容器验证。后续应优先修复项目空间列表 total、集群资源归属第三段和域名检查文案/状态。

---

# 场景测试问题记录
## 记录规则

- 每次场景测试都新增一个批次，标题格式为 `YYYY-MM-DD 场景名称`。
- 每个问题都记录：现象、影响、处理状态、修复动作、复测结果。
- 修复完成但未复测时，状态保持 `待复测`。
- 确认不是缺陷但会影响体验时，状态使用 `体验待优化`。
- 被产品决策接受的限制，状态使用 `已接受`，并记录原因。
- 涉及后续开发计划的问题，同步回填 `TODO.md`。

## 状态说明

| 状态 | 含义 |
| --- | --- |
| 待处理 | 已确认问题，尚未开始修复 |
| 修复中 | 已开始改动，但未完成验证 |
| 待复测 | 已修复，等待场景回归 |
| 已复测 | 已通过同类场景验证 |
| 体验待优化 | 不阻断主路径，但影响用户体验 |
| 已接受 | 当前阶段接受该限制，不安排立即修复 |

## 2026-06-11 Chrome 本地核心 DevOps 验证

### 测试范围

- 登录和看板。
- 应用详情基础浏览。
- 构建列表浏览。
- 访问入口创建。
- 集群资源归属展示。

### 问题记录

| ID | 问题 | 影响 | 状态 | 修复动作 | 复测结果 |
| --- | --- | --- | --- | --- | --- |
| ST-20260611-001 | 新应用创建失败，后端报 `applications.source_type` 非空约束错误 | 阻断新建应用和后续部署配置验证 | 待复测 | 已清理应用表旧来源字段残留，应用创建只依赖名称、标识和图标 | 待在清库后的完整场景中复测 |
| ST-20260611-002 | 应用详情缺少创建部署配置入口 | 阻断“应用 -> 部署配置 -> 构建/部署”主路径 | 待复测 | 应用部署页已补齐部署配置新增、编辑、删除入口，支持 repository / image 来源 | 待在 repository 构建场景中复测 |
| ST-20260611-003 | 现有部署配置数据不完整，触发构建按钮禁用 | 旧数据无法验证新模型构建链路 | 已接受 | 平台尚未上线，旧数据不做兼容；要求清库或用 UI 重建新模型数据 | Neo Blog 批次已通过 UI 重建项目、环境、应用和镜像部署配置 |
| ST-20260611-004 | 历史 BuildRun / Release 无部署配置归属 | 旧构建和发布无法在新部署页正确归属 | 已接受 | 不做历史数据兼容；场景验证必须使用带 `deploymentTargetId` 的新数据 | 待 repository 构建和自动部署场景复测 |
| ST-20260611-005 | 顶部“创建发布”和行内“部署”状态不一致 | 用户可能打开无法保存的发布弹窗 | 已复测 | 创建发布入口按新模型收紧，只允许可发布部署配置进入；顶部入口显式传入默认部署配置 | Neo Blog 批次已验证顶部发布弹窗会选中部署配置和环境 |
| ST-20260611-006 | 创建应用弹窗描述仍是旧 sourceType 口径 | 用户误解应用创建阶段需要选择来源类型 | 待复测 | 已改为应用只维护基础信息，来源和交付归属部署配置 | 待下一轮应用创建场景复测 |
| ST-20260611-007 | 旧 Kubernetes 资源部署配置显示为 `-` | 旧资源归属不完整 | 已接受 | 移除 legacy fallback 后的预期结果；后续通过 UI 清理旧资源或重建本地集群资源 | Neo Blog 批次暴露了孤儿资源清理入口仍缺失 |
| ST-20260611-008 | 看板图钉按钮泄漏 i18n key | 影响界面可信度 | 待复测 | 已补齐对应 i18n 文案 | 待下一轮看板场景复测 |
| ST-20260611-009 | 应用概览部署配置指标重复 | 用户可能误解有两类部署配置 | 待复测 | 已合并重复指标 | 待下一轮应用概览场景复测 |

## 2026-06-11 Neo Blog 浏览器部署试跑

### 测试范围

- 只通过浏览器模拟用户操作。
- 通过 UI 清理旧项目空间数据。
- 创建 `Neo Blog 场景测试` 项目空间、`dev` 环境、前端 / 后端 / 数据库应用。
- 创建镜像来源部署配置并尝试发布数据库。

### 问题记录

| ID | 问题 | 影响 | 状态 | 修复动作 | 复测结果 |
| --- | --- | --- | --- | --- | --- |
| ST-20260611-010 | 清库后仓库绑定无法通过 UI 重建 | 阻断 `snowykami/neo-blog` repository 构建路径，只能临时走镜像直部署 | 待处理 | 已写入 `TODO.md`：补齐仓库绑定入口 | 未修复 |
| ST-20260611-011 | 项目空间删除后列表空状态和分页总数不一致 | 用户会误以为仍有残留数据 | 待处理 | 待修复项目空间列表删除后的分页和查询缓存刷新 | 未修复 |
| ST-20260611-012 | 顶部“创建发布”可能创建无部署配置归属的发布，生成兜底资源名 `dplt` | 产生孤儿 Kubernetes 资源，并可能和后续部署配置冲突 | 已复测 | 顶部创建发布按钮改为显式使用第一个可发布部署配置打开弹窗 | 浏览器验证弹窗已选中 `数据库开发`、`开发环境` 和镜像引用 |
| ST-20260611-013 | 集群资源页缺少孤儿资源清理入口 | 错误发布产生的 Deployment / Service 无法只通过 UI 删除 | 待处理 | 已写入 `TODO.md`：补齐集群资源清理与重新同步入口 | 未修复 |
| ST-20260611-014 | 数据库镜像发布 rollout 长时间 `ready=0`，页面缺少 Pod events 和容器日志 | 无法判断是镜像拉取、调度、端口还是启动参数问题 | 待处理 | 已写入 `TODO.md`：补齐部署失败诊断 | 未修复 |
| ST-20260611-015 | 应用服务端口被 UI 固定为 8080 | Neo Blog 前端监听 3000、后端可配置 80，默认 8080 会导致 Service 和访问入口转发失败 | 待复测 | 应用创建/编辑和应用详情基础表单已恢复服务端口字段 | 待 Neo Blog 重跑验证 |

### 修复记录

- 项目空间工作台新增“仓库绑定”tab，可从项目空间内直接创建和维护 RepositoryBinding。
- 项目空间删除后会收敛分页并主动刷新项目空间分页查询，避免空状态和总数不一致。
- 集群资源页新增平台管理资源删除入口，并通过后端校验 `luna.devops/managed-by` 标签后删除。
- Pod 摘要补充容器 waiting/terminated reason 和 Pod condition，部署卡住时不再只显示 `ready 0/1`。
- 应用基础表单恢复服务端口字段，用于正确生成 Deployment/Service/Ingress 端口。

### 本轮结论

清库后的 UI 重建路径可以创建项目空间、环境、应用和镜像来源部署配置。首次试跑未跑通，主要阻塞是仓库绑定入口、集群资源清理入口和部署失败诊断能力；后续修复后已在“Neo Blog 清库重跑验收”批次完成镜像直部署闭环。

## 2026-06-11 Neo Blog 清库重跑验收

### 测试范围

- 主人确认允许清库后，清理本地业务数据、Redis 队列和旧 Kubernetes 残留资源。
- 使用浏览器重建项目空间、环境、数据库 / 后端 / 前端应用。
- 通过浏览器创建镜像来源部署配置、发布、访问入口。
- 验证 `blogtest.local` 通过本地 minikube ingress 可访问。

### 问题记录

| ID | 问题 | 影响 | 状态 | 修复动作 | 复测结果 |
| --- | --- | --- | --- | --- | --- |
| ST-20260611-010 | 清库后仓库绑定无法通过 UI 重建 | 阻断 repository 构建路径 | 已复测 | 项目空间工作台新增“仓库绑定”tab | 工作台可进入仓库绑定入口；本轮因公共镜像架构问题改走镜像直部署验收 |
| ST-20260611-011 | 项目空间删除后列表空状态和分页总数不一致 | 用户会误以为仍有残留数据 | 已复测 | 删除成功后收敛分页并刷新项目空间分页查询；空列表总页数展示为 1 | 清库后项目空间列表空状态展示正常 |
| ST-20260611-013 | 集群资源页缺少孤儿资源清理入口 | 错误资源无法通过 UI 删除 | 已复测 | 集群资源页新增平台管理资源删除入口，后端校验 managed-by 标签后删除 | 旧 `dplt` Deployment / Service 已清理，资源列表不再混入旧命名残留 |
| ST-20260611-014 | 部署失败诊断信息不足 | 用户无法判断 rollout stuck 原因 | 已复测 | Pod 摘要补充 waiting/terminated reason 和 Pod condition | 后端失败时资源页展示 `ImagePullBackOff`，kubectl events 确认为镜像缺少 `linux/arm64` manifest |
| ST-20260611-015 | 应用服务端口被 UI 固定为 8080 | 前端 3000、数据库 5432、后端 80 无法正确转发 | 已复测 | 应用创建/编辑和详情基础表单恢复服务端口字段 | 数据库 5432、后端 80、前端 3000 均生成正确 Service |
| ST-20260611-016 | `snowykami/neo-blog-backend:latest` 公共镜像缺少 `linux/arm64` manifest | ARM minikube 无法直接拉取后端镜像 | 已接受 | 本地从源码构建 `neo-blog-backend:devtest` 并加载到 minikube；TODO 增加镜像架构不匹配提示优化 | 后端使用本地 ARM 镜像发布成功 |
| ST-20260611-017 | minikube ingress addon 使用的阿里云镜像 digest 不存在 | Ingress controller 长期 `ImagePullBackOff`，域名无法访问 | 已接受 | 将 ingress controller 镜像切到 `registry.k8s.io/ingress-nginx/controller:v1.14.3`，并复用已有 admission secret | Ingress controller Running，GatewayRoute 下发后可访问 |
| ST-20260611-018 | 部署配置编辑保存后列表未立即刷新目标镜像 | 用户会看到旧镜像，误以为保存未生效 | 体验待优化 | 暂未修复，已作为体验卡点记录 | 刷新或创建发布弹窗能带出更新后的镜像；不阻断本轮验收 |
| ST-20260612-001 | 部署页只能看部署日志，不能看运行中容器日志 | 排障时无法直接确认应用启动后的 stdout/stderr | 已复测 | 部署日志弹窗新增“部署日志 / 运行日志”切换，运行日志由后端按 Release 定位当前 Pod 并读取容器日志 | 2026-06-14 浏览器闭环验证中，nginx 发布可在“运行日志”看到 Pod、容器和 nginx 启动日志 |
| ST-20260612-002 | 缺少类似 `kubectl exec` 的 Web Console | 用户需要离开平台才能进入容器查看环境变量、文件和进程状态 | 已复测 | 已升级为 `xterm.js + WebSocket + Kubernetes exec TTY` 真实交互式终端，并写审计日志 | 2026-06-14 浏览器闭环验证中，Web Console 已连接容器 shell，可读取环境变量和挂载的 ConfigMap 文件 |

### 修复与环境处理记录

- 清理业务表、Redis 队列和旧 Kubernetes 资源后，通过浏览器重建 `Neo Blog 验收` 项目空间、`开发环境`、数据库 / 后端 / 前端应用。
- 数据库部署配置使用 `postgres:16-alpine`，服务端口 `5432`，发布成功。
- 后端根据 Neo Blog `.env.example` 配置 `PORT=80`、`DB_DRIVER=postgres`、`DB_HOST=dplt-f5a2cceb13` 等变量；公共镜像缺少 ARM manifest，改用本地构建并加载的 `neo-blog-backend:devtest`，发布成功。
- 前端根据 Next standalone 配置 `PORT=3000`、`HOSTNAME=0.0.0.0`、`BACKEND_URL=http://dplt-5540420c06:80`；使用本地构建并加载的 `neo-blog-frontend:devtest`，发布成功。
- 访问入口绑定前端部署配置，域名 `blogtest.local`，路径 `/`，TLS 模式 `http-only`。
- 本地 minikube 容器已有宿主机映射 `127.0.0.1:64111 -> node:32443`，将 ingress-nginx HTTP NodePort 调整为 `32443` 后，无需额外 port-forward 即可访问。

### 复测结果

- Kubernetes 资源：
  - `dplt-f5a2cceb13` Postgres Deployment / Service：`1/1 Running`。
  - `dplt-5540420c06` 后端 Deployment / Service：`1/1 Running`。
  - `dplt-6cdf13c21b` 前端 Deployment / Service：`1/1 Running`。
  - `blogtest.local` Ingress 已下发，后端指向 `dplt-6cdf13c21b:3000`。
- HTTP 验证：`curl -I --resolve blogtest.local:64111:127.0.0.1 http://blogtest.local:64111/` 返回 `200 OK`。
- Chrome 验证：访问 `http://blogtest.local:64111/`，页面标题为 `首页 - Snowykami's Blog`，页面展示“最新文章”。

### 本轮结论

Neo Blog 核心镜像直部署链路已跑通：项目空间、环境、应用、部署配置、发布、Service、Ingress 和本地域名访问均可完成。repository 构建、自动部署、回滚、ConfigMap/Secret 编辑和更完整的 Pod events / 日志诊断仍留待后续场景覆盖。

## 2026-06-14 Browser 镜像直部署运行配置闭环

### 测试范围

- 使用 Codex in-app Browser 访问本地前端 `http://localhost:5173`。
- 使用已登录的 `Platform Admin <admin@luna.dev>` 会话。
- 从 UI 新建项目空间、环境、应用、镜像来源部署配置、发布和访问入口。
- 验证运行日志、Web Console、ConfigMap 环境变量、ConfigMap 文件、Secret 不回显、集群资源归属和 ingress 访问。

### 问题记录

| ID | 问题 | 影响 | 状态 | 修复动作 | 复测结果 |
| --- | --- | --- | --- | --- | --- |
| ST-20260614-001 | 项目空间列表空状态和分页总数再次不一致 | 用户看到“还没有项目空间”，但分页显示“共 1 条”；新建一个项目空间后只显示 1 行却显示“共 2 条” | 待复测 | 项目空间列表查询显式过滤 `projects.deleted_at is null`，避免 `Table("projects")` 绕过 GORM 软删除过滤后把已删除项目计入 total | 待重启 API 后浏览器复测项目空间列表 |
| ST-20260614-002 | 集群资源归属只展示“项目空间 / 应用”，缺少部署配置 | 用户无法从 Deployment、Pod、Service、Ingress、ConfigMap、Secret 列表直接判断资源属于哪个部署配置 | 待复测 | 集群资源归属展示补齐第三段部署配置名称；名称缺失时回退展示 `deploymentTargetId` | 待浏览器复测集群资源页 |
| ST-20260614-003 | 已创建域名点击“检查域名”提示“域名已被占用” | 用户会误以为当前访问入口配置错误；实际 ingress 已下发且访问可通 | 待复测 | 域名检查接口增加 `routeId` 上下文并返回 `available/current/conflict`，前端对当前路由显示“当前域名属于此访问入口” | 待重启 API 后浏览器复测访问页检查域名 |
| ST-20260614-004 | in-app Browser 直接打开 `nginx-loop.local:64111` 被 `ERR_BLOCKED_BY_CLIENT` 拦截 | 无法只靠浏览器直接访问自定义本地域名完成最终页面验收 | 已接受 | 本地环境限制；本轮使用 `curl --resolve nginx-loop.local:64111:127.0.0.1` 验证 ingress HTTP 内容 | `curl` 返回 `luna browser loop ok`，平台链路已确认可访问 |

### 已完成操作

1. 创建项目空间 `浏览器闭环测试`，标识 `browser-loop-test`。
2. 创建环境 `开发环境`，标识 `dev`，阶段 `开发`，集群 `Local Minikube`。
3. 创建应用 `Nginx 演示`，标识 `nginx-demo`。
4. 创建镜像来源部署配置 `开发部署`：
   - 镜像：`nginx:1.27-alpine`。
   - 服务端口：`80`。
   - 普通配置：`APP_ENV=dev`、`FEATURE_FLAG=true`。
   - 附加 ConfigMap：`NGINX_ENTRYPOINT_QUIET_LOGS=1`。
   - ConfigMap 文件：`/etc/nginx/conf.d/default.conf`，返回 `luna browser loop ok`。
5. 创建 Release `#1`，发布成功。
6. 在部署页查看部署日志和运行日志。
7. 打开 Web Console，进入容器 shell，验证环境变量和 ConfigMap 文件已经生效。
8. 创建访问入口 `nginx-loop.local`，路径 `/`，TLS 模式 `http-only`。
9. 在集群资源页查看 Workload、Service、Ingress、ConfigMap、Secret。

### 验收结果

- Kubernetes 资源：
  - Namespace：`ns-3948b72e61`。
  - Deployment / Service：`dplt-8d88907683`，`1/1 Running`，端口 `80`。
  - ConfigMap：`dplt-8d88907683-config` 有 3 个 key。
  - ConfigMap 文件：`dplt-8d88907683-config-files` 有 1 个 key。
  - Secret：`dplt-8d88907683-secret`，页面只展示 `data hidden`。
  - Ingress：`luna-gateway-gwr-2a1118d43c237ea5d514591d`，Host 为 `nginx-loop.local`。
- 发布页：
  - 运行状态为“就绪”。
  - 发布状态为“成功”。
  - 内网访问展示 `dplt-8d88907683.ns-3948b72e61.svc.cluster.local`。
  - 部署日志展示命名空间、ConfigMap/Secret、Deployment/Service 和 rollout 过程。
  - 运行日志展示 Pod `dplt-8d88907683-6554fb8995-md5tg` / 容器 `app` 的 nginx 启动日志。
- Web Console：
  - 成功连接真实容器终端。
  - 命令输出包含 `APP_ENV=dev FEATURE_FLAG=true NGINX_ENTRYPOINT_QUIET_LOGS=1`。
  - 能读取 `/etc/nginx/conf.d/default.conf` 中的 `luna browser loop ok`。
- 访问验证：
  - `curl --resolve nginx-loop.local:64111:127.0.0.1 http://nginx-loop.local:64111/` 返回 `luna browser loop ok`。
  - `http://127.0.0.1:64111/` 返回 ingress 默认 `404`，符合缺少 Host 的预期。

### 本轮结论

镜像直部署、运行配置、ConfigMap 文件、Release、运行日志、Web Console、Service、Ingress 和 HTTP 访问闭环已跑通。剩余主要卡点是项目空间列表分页 total 异常、集群资源归属缺部署配置名称、域名检查提示不区分当前路由自身占用。
